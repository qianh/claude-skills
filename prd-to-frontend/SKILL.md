---
name: prd-to-frontend
description: 自动化前端开发助手，将 PRD（产品需求文档）和 Swagger API 文档转换为 React + Ant Design 前端代码。当用户提供 PRD 截图/文档 + Swagger JSON/接口定义，需要自动生成前端页面代码时使用。支持：(1) 解析 PRD 截图识别 UI 组件和布局 (2) 解析 Swagger 生成 TypeScript 类型和 API 调用层 (3) 基于工程规范生成页面组件代码 (4) 表单、表格、弹窗等常见业务组件的智能生成。触发词：PRD转代码、需求转前端、自动生成页面、Swagger生成代码。
---

# PRD to Frontend 自动化开发助手

将 PRD + Swagger + 开发规范 自动转换为前端代码。

---

## 第一部分：输入收集

在开始任务之前，需要收集以下信息：

### 1.1 必需输入

| 序号 | 输入项 | 说明 | 示例 |
|------|--------|------|------|
| ① | **PRD 文档** | 文件路径、URL 或上传的文件 | `/path/to/prd.pdf` 或 钉钉文档链接 |
| ② | **Swagger 接口** | 接口地址或 JSON 文件路径 | `https://xxx/v3/api-docs` 或 `/path/to/swagger.json` |
| ③ | **开发规范文档** | 项目根目录的 `DEVELOPMENT_GUIDE.md` | 自动查找或用户提供 |
| ④ | **代码生成路径** | 生成代码的目标目录 | 默认: `{项目根目录}/src/pages/` |
| ⑤ | **其他要求** | 用户的特殊需求（可选） | "使用 ProTable"、"不需要样式文件" |

### 1.2 输入收集流程

```
┌─────────────────────────────────────────────────────────────────┐
│  PRD to Frontend - 输入信息收集                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ① PRD 文档路径/链接:                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ {等待用户输入}                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ② Swagger 接口地址/文件路径:                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ {等待用户输入}                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ③ 开发规范文档:                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ {自动查找 DEVELOPMENT_GUIDE.md，未找到则提示用户提供}       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ④ 代码生成路径:                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ /src/pages/  (默认值，可修改)                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ⑤ 其他要求（可选）:                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ {用户特殊需求}                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 输入验证

收集完成后，验证各输入是否有效：

- [ ] PRD 文档可访问/可读取
- [ ] Swagger 接口可访问，返回有效的 OpenAPI 格式
- [ ] 开发规范文档存在且可读取
- [ ] 代码生成路径有效

**如果开发规范文档未找到**：
```
⚠️ 未在项目根目录找到 DEVELOPMENT_GUIDE.md
请提供开发规范文档路径，或输入 "skip" 使用默认规范（生成的代码可能需要手动调整）
```

---

## 第二部分：PRD 需求解析

### 2.1 解析 PRD 文档

读取 PRD 文档，识别并提取：
- 页面名称和功能描述
- UI 布局结构（表格、表单、弹窗等）
- 字段列表（名称、类型、是否必填）
- 交互逻辑（按钮操作、校验规则）

### 2.2 任务拆分

将 PRD 拆分为独立的**需求任务**，每个任务对应一个功能点：

```yaml
tasks:
  - id: T001
    name: "列表查询"
    type: "query"
    description: "分页查询配置列表，支持按公司筛选"
    ui_elements:
      - 搜索表单
      - 数据表格
      - 分页组件
    fields:
      - { label: "适用公司", type: "SearchSelect", required: false }
    
  - id: T002
    name: "新增配置"
    type: "create"
    description: "新增订舱智能体配置"
    ui_elements:
      - 弹窗表单
    fields:
      - { label: "适用公司", type: "SearchSelect", required: true }
      - { label: "支持船公司", type: "Select[multiple]", required: false }
      - { label: "邮箱Host", type: "Input", required: false }
      # ...
    
  - id: T003
    name: "编辑配置"
    type: "update"
    description: "编辑已有配置"
    ui_elements:
      - 弹窗表单（同新增）
      - 详情回显
    
  - id: T004
    name: "删除配置"
    type: "delete"
    description: "单条/批量删除配置"
    ui_elements:
      - 确认弹窗
```

### 2.3 输出需求任务文档

**必须输出 `PRD_TASKS.md` 到项目根目录**：

```markdown
# PRD 需求任务解析报告

> 生成时间: {timestamp}
> PRD 来源: {prd_source}

## 1. 页面概述

| 项目 | 值 |
|------|-----|
| 页面名称 | 订舱智能体配置 |
| 功能模块 | BookingConfig |
| 页面描述 | 管理订舱智能体的配置信息，包括船公司、邮箱、映射等配置 |

## 2. 需求任务清单

### T001: 列表查询

| 属性 | 值 |
|------|-----|
| 任务类型 | query |
| 功能描述 | 分页查询配置列表，支持按公司筛选 |
| UI 组件 | 搜索表单、数据表格、分页 |

**搜索字段：**
| 字段名 | 组件类型 | 必填 |
|--------|---------|------|
| 适用公司 | SearchSelect | 否 |

**列表字段：**
| 字段名 | 组件类型 | 说明 |
|--------|---------|------|
| 适用公司 | Link | 点击进入编辑 |
| 支持船公司 | Tag[] | 多标签展示 |
| 最近修改人 | Text | |
| 最近修改时间 | Text | |
| 操作 | Actions | 编辑、删除 |

**待绑定接口：** `_________________` (待填写)

---

### T002: 新增配置

| 属性 | 值 |
|------|-----|
| 任务类型 | create |
| 功能描述 | 新增订舱智能体配置 |
| UI 组件 | Modal + Form |

**表单字段：**
| 字段名 | 组件类型 | 必填 | 校验规则 |
|--------|---------|------|---------|
| 适用公司 | SearchSelect | ✅ | required |
| 支持船公司 | Select[multiple] | | |
| 邮箱Host | Input | | |
| 邮箱Port | InputNumber | | min:1, max:65535 |
| 邮箱账号 | Input | | |
| 邮箱密码 | Input.Password | | |
| ... | | | |

**待绑定接口：** `_________________` (待填写)

---

### T003: 编辑配置

| 属性 | 值 |
|------|-----|
| 任务类型 | update |
| 功能描述 | 编辑已有配置，先查询详情再提交更新 |
| UI 组件 | Modal + Form（复用新增表单） |

**待绑定接口（详情）：** `_________________` (待填写)
**待绑定接口（更新）：** `_________________` (待填写)

---

### T004: 删除配置

| 属性 | 值 |
|------|-----|
| 任务类型 | delete |
| 功能描述 | 单条删除或批量删除配置 |
| UI 组件 | Popconfirm / Modal.confirm |

**待绑定接口：** `_________________` (待填写)

---

## 3. 待确认事项

- [ ] {需要确认的问题}

---

**请检查以上任务拆分是否正确，确认后进入接口绑定环节。**
```

---

## 第三部分：Swagger 接口获取

### 3.1 获取 Swagger 文档

从用户提供的地址/文件获取 Swagger JSON，解析：
- 所有可用的 API 接口（paths）
- 所有数据模型（schemas）
- 接口分组（tags）

### 3.2 筛选相关接口

根据 PRD 内容，智能筛选可能相关的接口：

```yaml
available_apis:
  - id: A001
    path: "/sys-booking-config/page"
    method: "POST"
    summary: "订舱智能体配置.列表查询"
    tag: "大掌柜订舱配置相关"
    request_body: "PageQuery"
    response: "Result<PageInfo<SysBookingConfigVO>>"
    
  - id: A002
    path: "/sys-booking-config/detail"
    method: "POST"
    summary: "订舱智能体配置.明细查询"
    parameters: [{ name: "id", in: "query", type: "integer" }]
    response: "Result<SysBookingConfigVO>"
    
  - id: A003
    path: "/sys-booking-config/insert"
    method: "POST"
    summary: "新增"
    request_body: "SysBookingConfigInsertParam"
    response: "Result<Long>"
    
  - id: A004
    path: "/sys-booking-config/update"
    method: "POST"
    summary: "更新"
    request_body: "SysBookingConfigUpdateParam"
    response: "Result<Long>"
    
  - id: A005
    path: "/sys-booking-config/deletes"
    method: "POST"
    summary: "批量删除"
    parameters: [{ name: "ids", in: "query", type: "array[integer]" }]
    response: "Result<Integer>"
    
  - id: A006
    path: "/booking-boss/cornerstone-fuzzy/bookingConfig/tenantId"
    method: "POST"
    summary: "模糊查询租户"
    parameters: [{ name: "searchValue", in: "query" }]
    response: "Result<List<SelectOption>>"
```

### 3.3 输出接口列表

展示给用户选择：

```markdown
## 可用接口列表

| ID | 路径 | 方法 | 描述 | 请求参数 | 响应类型 |
|----|------|------|------|---------|---------|
| A001 | /sys-booking-config/page | POST | 列表查询 | PageQuery | PageResult<VO> |
| A002 | /sys-booking-config/detail | POST | 明细查询 | id | VO |
| A003 | /sys-booking-config/insert | POST | 新增 | InsertParam | Long |
| A004 | /sys-booking-config/update | POST | 更新 | UpdateParam | Long |
| A005 | /sys-booking-config/deletes | POST | 批量删除 | ids[] | Integer |
| A006 | /booking-boss/.../tenantId | POST | 租户模糊搜索 | searchValue | SelectOption[] |
| ... | | | | | |
```

---

## 第四部分：需求-接口关联（核心）

### 4.1 关联交互流程

**这是最关键的步骤**，需要用户确认每个需求任务对应的接口。

采用**逐任务确认**的交互方式：

```
┌─────────────────────────────────────────────────────────────────┐
│  需求-接口关联                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📋 任务 T001: 列表查询                                          │
│  ────────────────────────────────────────────────────────────   │
│  功能：分页查询配置列表，支持按公司筛选                              │
│                                                                 │
│  请选择该任务需要的接口（可多选，用逗号分隔）:                        │
│                                                                 │
│  推荐接口:                                                       │
│  ✓ [A001] /sys-booking-config/page - 列表查询                   │
│    [A006] /booking-boss/.../tenantId - 租户搜索（搜索下拉用）      │
│                                                                 │
│  其他可选接口:                                                    │
│    [A002] /sys-booking-config/detail                            │
│    [A003] /sys-booking-config/insert                            │
│    ...                                                          │
│                                                                 │
│  请输入接口 ID: A001, A006                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ A001, A006                                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  [确认] [跳过此任务] [返回上一步]                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 关联规则

- **一个任务可以绑定多个接口**（如：列表查询 = 分页接口 + 下拉搜索接口）
- **一个接口可以被多个任务使用**（如：详情接口 被 编辑、查看 共用）
- 系统会**智能推荐**可能匹配的接口，但最终由用户确认
- 如果某任务不需要接口，可以跳过

### 4.3 智能推荐逻辑

```yaml
recommendation_rules:
  - task_type: "query"
    recommend_patterns:
      - path_contains: "/page"
      - path_contains: "/list"
      - summary_contains: "查询"
      
  - task_type: "create"
    recommend_patterns:
      - path_contains: "/insert"
      - path_contains: "/add"
      - path_contains: "/create"
      - summary_contains: "新增"
      
  - task_type: "update"
    recommend_patterns:
      - path_contains: "/update"
      - path_contains: "/edit"
      - path_contains: "/detail"  # 需要先查详情
      - summary_contains: "更新"
      
  - task_type: "delete"
    recommend_patterns:
      - path_contains: "/delete"
      - path_contains: "/remove"
      - summary_contains: "删除"
      
  - field_type: "SearchSelect"
    recommend_patterns:
      - path_contains: "/fuzzy"
      - path_contains: "/search"
      - path_contains: "/options"
      - response_contains: "SelectOption"
```

### 4.4 关联完成后输出

更新 `PRD_TASKS.md`，填入已关联的接口：

```markdown
### T001: 列表查询

**已绑定接口：**
| 接口 ID | 路径 | 用途 |
|---------|------|------|
| A001 | /sys-booking-config/page | 分页查询 |
| A006 | /booking-boss/.../tenantId | 公司下拉搜索 |

---

### T002: 新增配置

**已绑定接口：**
| 接口 ID | 路径 | 用途 |
|---------|------|------|
| A003 | /sys-booking-config/insert | 提交新增 |
| A006 | /booking-boss/.../tenantId | 公司下拉搜索 |
```

### 4.5 字段映射验证

关联接口后，自动进行**字段映射验证**：

```
┌─────────────────────────────────────────────────────────────────┐
│  字段映射验证 - T002: 新增配置                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  PRD 字段 → Swagger 字段映射:                                     │
│                                                                 │
│  ✅ 适用公司 → gm1OrgId (integer, required)                      │
│  ✅ 支持船公司 → supportCarrierCode (array[string])              │
│  ✅ 邮箱Host → host (string)                                     │
│  ✅ 邮箱Port → port (integer)                                    │
│  ⚠️ 邮箱账号 → account (string) [名称不完全匹配，请确认]            │
│  ✅ 邮箱密码 → password (string)                                  │
│                                                                 │
│  ❌ 未映射的 PRD 字段:                                            │
│     - "配置类型" (Swagger 中未找到对应字段)                         │
│                                                                 │
│  ❌ 未使用的 Swagger 字段:                                        │
│     - sslEnable, protocol, entrustDocDocumentId, ...            │
│                                                                 │
│  [确认映射] [手动调整] [跳过验证]                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 第五部分：代码生成

### 5.1 生成前确认

```
┌─────────────────────────────────────────────────────────────────┐
│  代码生成确认                                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📁 生成路径: /Users/hong/project/src/pages/BookingConfig/       │
│                                                                 │
│  📄 将生成以下文件:                                               │
│     ├── index.tsx          (页面主组件)                          │
│     ├── index.module.less  (样式文件)                            │
│     ├── api.ts             (API 调用层)                          │
│     ├── type.ts            (类型定义)                            │
│     └── components/                                             │
│         └── ConfigModal.tsx (编辑弹窗)                           │
│                                                                 │
│  📋 使用的开发规范:                                               │
│     - 表格组件: StandardTable                                    │
│     - 弹窗管理: useModalSet                                      │
│     - 请求封装: @/service/request                                │
│                                                                 │
│  [开始生成] [修改路径] [返回上一步]                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 代码生成规则

**严格遵循开发规范**：

1. **文件结构**：按 DEVELOPMENT_GUIDE.md 要求的目录结构生成
2. **导入路径**：使用规范中定义的路径（@/service/request, @/components/...）
3. **组件使用**：使用规范中推荐的组件（StandardTable, SearchSelect, useModalSet）
4. **类型定义**：严格从 Swagger Schema 生成，不添加不存在的字段
5. **字段名称**：使用 Swagger 中的字段名，不可臆造

### 5.3 生成文件清单

```
{OutputPath}/
├── index.tsx              # 页面主组件
├── index.module.less      # 样式文件
├── api.ts                 # API 调用层
├── type.ts                # 类型定义（注意：不是 types.ts）
├── constants.ts           # 常量定义（如有枚举）
└── components/            # 子组件目录
    └── ConfigModal.tsx    # 编辑弹窗组件
```

### 5.4 生成代码头部注释

每个文件开头添加：

```typescript
/**
 * 订舱智能体配置 - {文件描述}
 * 
 * @description 基于 PRD 和 Swagger 自动生成
 * @prd {PRD来源}
 * @swagger {Swagger来源}
 * @generated {生成时间}
 * 
 * 关联任务: T001(列表查询), T002(新增), T003(编辑), T004(删除)
 * 关联接口: A001, A002, A003, A004, A005, A006
 */
```

---

## 第六部分：验证清单

代码生成后，执行以下验证：

### 6.1 编译检查
- [ ] TypeScript 类型正确，无 any 滥用
- [ ] 所有导入路径有效
- [ ] 组件 Props 类型正确
- [ ] 没有语法错误

### 6.2 字段检查
- [ ] 所有 dataIndex 来自 Swagger Schema
- [ ] 所有 Form.Item name 来自 Swagger Param
- [ ] 必填校验与 Swagger required 一致

### 6.3 规范检查
- [ ] 文件命名符合规范（type.ts 不是 types.ts）
- [ ] 导入路径符合规范
- [ ] 组件使用方式符合规范

### 6.4 输出验证报告

```markdown
# 代码生成验证报告

## 生成结果

| 文件 | 状态 | 说明 |
|------|------|------|
| index.tsx | ✅ 成功 | 页面主组件 |
| index.module.less | ✅ 成功 | 样式文件 |
| api.ts | ✅ 成功 | 6 个 API 函数 |
| type.ts | ✅ 成功 | 12 个类型定义 |
| components/ConfigModal.tsx | ✅ 成功 | 编辑弹窗 |

## 验证结果

- ✅ 类型检查通过
- ✅ 导入路径正确
- ✅ 字段映射完整
- ⚠️ 1 个 PRD 字段未映射（配置类型）

## 后续建议

1. 运行 `npm run tsc` 进行完整类型检查
2. 检查未映射的字段是否需要处理
3. 根据实际需求调整样式
```

---

## 附录：常见问题处理

### Q1: Swagger 接口访问失败
```
⚠️ 无法访问 Swagger 地址: https://xxx/v3/api-docs
可能原因: 网络问题、需要认证、地址错误

请选择:
1. 重新输入地址
2. 上传 Swagger JSON 文件
3. 手动输入接口信息
```

### Q2: PRD 字段无法映射
```
⚠️ 以下 PRD 字段在 Swagger 中未找到对应:
- "配置类型"

请选择:
1. 忽略此字段
2. 手动指定 Swagger 字段名
3. 此字段为纯前端字段（不需要接口）
```

### Q3: 开发规范文档缺失
```
⚠️ 未找到 DEVELOPMENT_GUIDE.md

请选择:
1. 提供规范文档路径
2. 使用默认规范（生成的代码可能需要调整）
```

---

## 参考文件

- `references/default-conventions.md` - 默认代码约定（无规范时使用）
- `references/swagger-patterns.md` - Swagger 解析模式
- `references/component-mapping.md` - UI 组件映射规则
