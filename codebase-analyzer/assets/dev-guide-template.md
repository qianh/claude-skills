# {项目名称} 开发指导手册

> 本手册由 AI 自动分析生成，用于指导后续开发工作。
> 生成时间：{生成时间}

---

## 1. 项目概览

### 1.1 技术栈

| 类别     | 技术                                        | 版本     |
| -------- | ------------------------------------------- | -------- |
| 框架     | {如 React/Vue/Next.js}                      | {版本号} |
| 语言     | {如 TypeScript/JavaScript}                  | {版本号} |
| 构建工具 | {如 Vite/Webpack}                           | {版本号} |
| 包管理器 | {如 pnpm/npm/yarn}                          | -        |
| 样式方案 | {如 Tailwind/CSS Modules/Styled-components} | {版本号} |
| 状态管理 | {如 Zustand/Redux/Pinia}                    | {版本号} |
| 后端框架 | {如 Express/Nest.js/Koa，若有}              | {版本号} |
| 数据库   | {如 PostgreSQL/MongoDB，若有}               | -        |

### 1.2 项目结构

```
{项目目录树，深度 2-3 层}
```

### 1.3 关键目录说明

| 目录              | 用途   |
| ----------------- | ------ |
| `src/components/` | {说明} |
| `src/pages/`      | {说明} |
| ...               | ...    |

---

## 2. 编码规范

### 2.1 命名规范

- **文件命名**：{如 kebab-case / PascalCase}
- **组件命名**：{如 PascalCase}
- **函数命名**：{如 camelCase}
- **常量命名**：{如 UPPER_SNAKE_CASE}
- **CSS 类命名**：{如 BEM / 无特定规范}

### 2.2 文件组织

{描述项目中组件/模块的文件组织方式}

示例：

```
ComponentName/
├── index.ts          # 导出
├── ComponentName.tsx # 主组件
├── ComponentName.module.css # 样式
└── types.ts          # 类型定义
```

### 2.3 导入顺序

{描述项目中 import 语句的排列顺序约定}

---

## 3. 组件模式

### 3.1 组件形式

{项目中使用的组件形式，可能有多种}

#### 形式 A：{名称，如"函数组件 + Hooks"}

```tsx
{代码示例，从项目中提取}
```

**使用场景**：{说明}

#### 形式 B：{名称，如有}

```tsx
{
  代码示例;
}
```

**使用场景**：{说明}

### 3.2 通用组件库

{项目使用的 UI 组件库及使用方式}

```tsx
{
  使用示例;
}
```

### 3.3 业务组件

{项目中的业务组件及其用法}

#### {组件名称 A}

- **路径**：`{文件路径}`
- **用途**：{说明}
- **Props**：

| 属性   | 类型   | 必填    | 说明   |
| ------ | ------ | ------- | ------ |
| {prop} | {type} | {是/否} | {说明} |

**使用示例**：

```tsx
{
  从项目中提取的实际使用代码;
}
```

---

## 4. 路由配置

### 4.1 路由方案

{描述项目的路由方案，如 React Router / Next.js App Router / Vue Router}

### 4.2 路由结构

| 路径     | 页面组件       | 说明     |
| -------- | -------------- | -------- |
| `/`      | `HomePage`     | 首页     |
| `/users` | `UserListPage` | 用户列表 |
| ...      | ...            | ...      |

### 4.3 路由定义方式

```tsx
{
  从项目中提取的路由配置代码示例;
}
```

### 4.4 动态路由 / 嵌套路由

{如有，说明动态路由和嵌套路由的使用方式}

---

## 5. 状态管理

### 5.1 方案概述

{描述项目的状态管理方案}

### 5.2 Store 结构

{描述 store 的组织方式}

### 5.3 使用示例

```tsx
{
  从项目中提取的状态管理使用示例;
}
```

---

## 6. API 调用

### 6.1 请求方案

{描述项目的 API 请求方案，如 Axios / Fetch / React Query}

### 6.2 请求封装

```tsx
{
  从项目中提取的请求封装代码;
}
```

### 6.3 使用示例

```tsx
{从项目中提取的 API 调用示例}
```

### 6.4 错误处理

{描述项目中的错误处理方式}

---

## 7. 样式方案

### 7.1 方案说明

{描述项目的样式方案}

### 7.2 使用示例

```tsx
{
  样式使用示例;
}
```

### 7.3 主题 / 设计变量

{如有，列出项目中的设计变量}

---

## 8. 多方案对比

{当同一场景存在多种实现方式时，在此记录}

### 8.1 {场景名称，如"列表页实现"}

#### 方案 A：{名称}

- **使用位置**：`{文件路径}`
- **特点**：{说明}

```tsx
{
  代码示例;
}
```

#### 方案 B：{名称}

- **使用位置**：`{文件路径}`
- **特点**：{说明}

```tsx
{
  代码示例;
}
```

#### 推荐选择

{根据项目中的使用频率或代码注释，给出推荐}

---

## 9. 开发约定

### 9.1 Git 提交规范

{如有，描述提交信息格式}

### 9.2 代码检查

{描述项目中的 ESLint / Prettier 配置}

### 9.3 测试规范

{如有，描述测试框架和测试文件组织方式}

---

## 10. 常见模式速查

### 10.1 新建页面

1. {步骤}
2. {步骤}
3. {步骤}

### 10.2 新建组件

1. {步骤}
2. {步骤}

### 10.3 添加 API 接口

1. {步骤}
2. {步骤}

---

## 附录

### A. 关键文件索引

| 用途     | 文件路径 |
| -------- | -------- |
| 入口文件 | `{路径}` |
| 路由配置 | `{路径}` |
| 全局样式 | `{路径}` |
| 环境配置 | `{路径}` |

### B. 依赖说明

{列出关键依赖及其用途}

| 依赖   | 用途   |
| ------ | ------ |
| {包名} | {用途} |
