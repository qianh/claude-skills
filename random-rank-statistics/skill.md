---
name: random-rank-statistics
description: 通过多次随机排序统计每个人名排在第一位的次数，按次数进行最终排序
arguments: required
---

# 随机排序统计分析工具

## Purpose

这个skill通过蒙特卡洛模拟方法，对给定的人名列表进行大量随机排序，统计每个人排在第一位的频率，从而得出基于概率的排序结果。

## When to Use This Skill

使用这个skill当：

- 需要对一组人名进行公平的随机排序
- 需要通过统计方法确定排序的优先级
- 需要消除人为偏见，通过大量随机采样得出客观排序
- 抽奖、随机分组等场景需要统计分析

## When NOT to Use This Skill

- 简单的随机排序（一次即可）
- 需要确定性排序结果的场景
- 按特定规则（如字母顺序、分数等）排序

## Instructions

1. **解析用户输入**
   - 从用户输入（ARGUMENTS）中提取参数
   - 格式：`名单,次数` 或 `名单|次数`
   - 示例：`张三,李四,王五|500` 或 `张三,李四,王五,1000`
   - 如果只提供名单，默认次数为 1000
   - 支持中文逗号、英文逗号、竖线分隔

2. **执行随机排序模拟**
   - 使用 Bash 工具执行 Python 脚本
   - 根据用户指定的次数进行随机排序
   - 每次排序后记录第一位的人名
   - 统计每个人名出现在第一位的次数

3. **生成统计结果**
   - 计算每个人名排在第一位的次数
   - 计算频率百分比
   - 按出现次数降序排列

4. **输出结果**
   - 显示输入的名单和模拟次数
   - 显示详细的统计数据（次数和百分比）
   - 显示最终排序结果

## Tools to Use

- **Bash**: 执行 Python 脚本进行随机排序和统计

## Example Output

```
📊 随机排序统计分析结果
=====================================
模拟次数: 1000 次
输入列表: 周恺琳, 张赟, 郭宇枫, 吴嘉辉, 王思杰, 沈文武

统计详情:
-------------------------------------
姓名        出现次数    频率
-------------------------------------
张赟          178       17.8%
周恺琳        165       16.5%
吴嘉辉        169       16.9%
王思杰        162       16.2%
郭宇枫        166       16.6%
沈文武        160       16.0%

最终排序 (按第一位出现次数):
1. 张赟
2. 吴嘉辉
3. 郭宇枫
4. 周恺琳
5. 王思杰
6. 沈文武
```

## Python Script Template

```python
import sys
import random
from collections import Counter

# 解析参数
def parse_input(input_str):
    """解析输入字符串，提取名单和次数"""
    # 尝试用 | 分隔
    if '|' in input_str:
        parts = input_str.split('|')
        names_part = parts[0]
        iterations = int(parts[1]) if len(parts) > 1 else 1000
    # 尝试用 , 分隔（最后一个是数字）
    else:
        parts = input_str.split(',')
        try:
            # 检查最后一个部分是否为数字
            iterations = int(parts[-1].strip())
            names_part = ','.join(parts[:-1])
        except ValueError:
            # 最后一个不是数字，则全部是名字
            names_part = input_str
            iterations = 1000

    # 处理名字列表（支持中英文逗号）
    names = [n.strip() for n in names_part.replace('，', ',').split(',') if n.strip()]

    return names, iterations

# 从命令行参数或默认值获取输入
input_str = ' '.join(sys.argv[1:]) if len(sys.argv) > 1 else "周恺琳,张赟,郭宇枫,吴嘉辉,王思杰,沈文武|1000"
names, iterations = parse_input(input_str)

# 执行随机排序
first_positions = []

for _ in range(iterations):
    shuffled = random.sample(names, len(names))
    first_positions.append(shuffled[0])

# 统计每个人排在第一位的次数
counter = Counter(first_positions)

# 输出结果
print(f"📊 随机排序统计分析结果")
print(f"{'='*40}")
print(f"模拟次数: {iterations} 次")
print(f"输入列表: {', '.join(names)}")
print()
print(f"统计详情:")
print(f"{'-'*40}")
print(f"{'姓名':<12} {'出现次数':<10} {'频率'}")
print(f"{'-'*40}")

for name, count in counter.most_common():
    percentage = (count / iterations) * 100
    print(f"{name:<12} {count:<10} {percentage:.1f}%")

print()
print(f"最终排序 (按第一位出现次数):")
for i, (name, _) in enumerate(counter.most_common(), 1):
    print(f"{i}. {name}")
```

## 使用示例

```bash
# 格式1: 名单|次数
python script.py "张三,李四,王五,赵六|500"

# 格式2: 名单,次数
python script.py "张三,李四,王五,赵六,500"

# 格式3: 只有名单（默认1000次）
python script.py "张三,李四,王五,赵六"

# 中文逗号也支持
python script.py "张三，李四，王五，赵六|2000"
```
